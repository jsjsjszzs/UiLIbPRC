Circular, stroke-free icon-only button for toolbars and compact actions.

```jsx
<IconButton icon={<CloseIcon/>} label="Close" variant="ghost" />
```

Variants: primary (brand fill), subtle (gray fill, default), ghost (transparent, reveals on hover).
