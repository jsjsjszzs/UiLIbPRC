Dark flat notification card with a tone dot; the only inverse-surface component in the system.

```jsx
<Toast tone="success" title="Saved" description="Your changes are live." onClose={()=>{}} />
```
