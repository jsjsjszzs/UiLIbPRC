Flat gray-filled container — depth comes only from the fill contrast against the app background, never a border or shadow.

```jsx
<Card><h3>Plan usage</h3><p>82% of storage used</p></Card>
```
Set `raised` for a white card on top of a gray section.
