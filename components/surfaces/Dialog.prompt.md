White flat modal over a dark scrim — the only component with a shadow, since it must separate from page content.

```jsx
<Dialog open={open} title="Delete file?" actions={<Button variant="danger">Delete</Button>}>This can't be undone.</Dialog>
```
